The following images are part of the Android Design Icon set:
	images/full_screen.png
	images/return_from_full_screen.png

To run on Node.js:
install Node.js 'https://nodejs.org/en/' (default is fine)
open terminal and navigate to project directory
run 'npm install'
run 'node index.js'
in browser, navigate to localhost:3000
